# Code Review Report

- 검토 일시: 2026-07-31 04:40:47 KST
- 대상 Commit: `2220843`
- 대상 Issue: #3
- 전체 결과: NEEDS_FIX

## Findings

| ID | Severity | Area | Description | Required Action |
| --- | --- | --- | --- | --- |
| CR-001 | MAJOR | DB schema | `rooms`, `room_members`, `room_invitations`의 PK가 자동 생성되지 않는다. 이후 방 생성/초대/멤버 추가 API에서 `id`를 직접 넣지 않으면 insert가 실패한다. 실제 검증에서도 `insert into rooms (name, description, type, owner_member_id) ...`가 `null value in column "id"`로 실패했다. | 런타임 생성 대상 테이블의 PK를 `generated by default as identity` 또는 `bigserial` 기반으로 바꾼다. seed 고정 ID와 충돌하지 않도록 sequence 시작값을 seed ID 이후로 조정하거나 seed insert 후 sequence를 보정한다. |
| CR-002 | MINOR | Verification | #3 범위의 Docker/health 검증은 충분하지만, 서버 코드에 최소 자동 테스트가 아직 없다. | #4 이후 API가 늘어나기 전에 health 또는 seed runner 단위의 최소 테스트 전략을 추가한다. |

## Requirement Coverage

| Requirement | Status | Evidence |
| --- | --- | --- |
| 백엔드 앱 초기화 | PASS | Spring Boot app, Gradle build, Docker server image |
| 프론트엔드 앱 초기화 | PASS | React/Vite app, `npm run build` pass |
| DB 연결 준비 | PASS | `/api/health` returned `{"status":"UP","database":"UP"}` |
| Docker 실행 구성 | PASS | `docker compose -f service/infra/docker-compose.yml config --quiet` pass |
| seed 최소 데이터 | PASS | members 4, rooms 3, room_members 6, notification_settings 1 |
| health check API | PASS | `GET /api/health` HTTP 200 |
| README 실행 방법 | PASS | Docker command and local URLs documented |
| 다음 기능 이슈 준비성 | NEEDS_FIX | Runtime insert 대상 PK 자동 생성 누락 |

## Review Decision

#3은 제출 실행 골격 자체는 정상 동작하지만, 이후 #5 방 생성 기능에서 바로 실패할 수 있는 DB schema 문제가 있다.

따라서 AI QA로 넘기지 않고 Implement 단계로 되돌린다.

## Recheck Criteria

- `rooms.id`, `room_members.id`, `room_invitations.id`가 런타임 insert 시 자동 생성된다.
- seed 데이터 재실행 후에도 중복되지 않는다.
- id 없이 방 생성 SQL이 성공한다.
- Docker 재실행 후 `/api/health`가 계속 HTTP 200을 반환한다.
