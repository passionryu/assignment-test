# Security Review Report

- 검토 일시: 2026-07-31 15:37 KST
- 대상 Commit: `c3694be`
- 대상 Issue: #4
- 재검토 배경: Human QA FAIL 이후 설정 화면 정의서 레이아웃 반영
- 전체 결과: PASS

## Security Findings

| ID | Severity | Risk | Status | Description |
| --- | --- | --- | --- | --- |
| SEC-004-001 | MINOR | MVP auth limitation | ACCEPTED | `X-Member-Id`는 실제 인증이 아니라 로컬 MVP 식별 헤더다. 이번 커밋에서 인증 헤더 처리 코드는 변경되지 않았으며, 실제 배포 인증으로 재사용하면 안 된다. |
| SEC-004-002 | MINOR | Password mock | ACCEPTED | 비밀번호 변경은 현재 비밀번호 검증/실제 해시 알고리즘 없이 MVP placeholder hash를 저장한다. 이번 커밋은 UI 재배치만 수행했고 기존 MVP 리스크를 악화시키지 않았다. |
| SEC-004-003 | MINOR | Disabled destructive action | ACCEPTED | 회원 탈퇴는 실제 API 없이 안내/placeholder로 유지된다. 클릭 핸들러가 없어 삭제 요청은 발생하지 않지만, 실제 구현 전까지 위험 액션임을 계속 명시한다. |

BLOCKER 또는 CRITICAL 보안 이슈는 없다.

## Security Checks

| Check | Result | Evidence |
| --- | --- | --- |
| 민감 정보 노출 | PASS | 실제 secret, API key, webhook URL 발견 없음 |
| 공개 저장소 금지 식별 문자열 | PASS | `docs`, `service`, `README.md`, `.gitignore` 기준 발견 없음 |
| Dependency audit | PASS | `npm audit --audit-level=moderate` returned `found 0 vulnerabilities` |
| API 호출 범위 | PASS | 새 fetch 경로 없음. 기존 프로필/비밀번호/알림 설정 API만 호출 |
| 이메일/전화번호 수정 방지 | PASS | 프로필 수정 모달에 이메일/전화번호 입력 없음 |
| 비밀번호 입력 로그 노출 | PASS | 프론트 입력값을 로그로 출력하는 코드 없음 |
| 회원 탈퇴 | PASS | 실제 API 호출 없이 placeholder로 유지 |
| 과도한 외부 연결 | PASS | 외부 webhook, third-party API, 배포 secret 추가 없음 |

## Secret Scan Notes

다음 항목은 secret scan 패턴에 걸렸지만 실제 민감 정보가 아니다.

- local-only dummy DB password
- 문서의 보안 컨벤션 설명
- API 명세의 password field 예시
- `package-lock.json`의 package name
- 코드의 password DTO/endpoint/column 명칭
- CSS class name의 `password-fields`

## Verification Commands

```bash
rg -n -i 'sweetbook|sweet book|스위트북|api\.sweetbook|myMentalCare|mymentalcare' docs service README.md .gitignore || true

rg -n -i '(secret|token|api[_ -]?key|webhook|password|passwd|pwd|authorization|bearer)' docs service README.md .gitignore || true

npm audit --audit-level=moderate
```

## Security Decision

Issue #4 Human QA FAIL 수정 커밋은 Review/Security 기준을 통과했다.

AI QA 단계로 이동 가능하다.
