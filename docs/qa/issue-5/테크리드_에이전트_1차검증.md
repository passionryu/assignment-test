# Tech Verification Report

- 대상 Issue: #5
- 상태: PASS
- 검증 일시: 2026-07-31
- 브랜치: feature-5

## 구현 요약

- `GET /api/rooms` API를 추가해 현재 사용자가 참여 중인 방 목록과 초대 대기 수를 조회한다.
- 앱 진입 시 프로필, 알림 설정, 방 목록을 함께 불러온다.
- 최상단 방을 기본 선택하고, 선택 방 기준으로 사이드바 하위 메뉴를 펼친다.
- 방 리스트 페이지 골격을 추가했다.
  - 방 생성
  - 초대 받은 방 조회
  - 참여 방 조회
  - 방 관리/수정 진입
- 채팅, 추억 게시판, 미션 인증, 편지는 상세 기능 없이 선택 방 기준 placeholder 화면만 제공한다.
- 전체 설정 진입은 기존 Issue #4 설정 화면을 유지한다.

## Build Verification

| 항목 | 결과 |
|---|---|
| `npm run build` | PASS |
| `./gradlew bootJar --no-daemon` | PASS |
| `docker compose -f service/infra/docker-compose.yml config --quiet` | PASS |
| `docker compose -f service/infra/docker-compose.yml up --build -d app-client app-server app-db` | PASS |
| `GET /api/health` | PASS: `{"status":"UP","database":"UP"}` |

## curl Verification

| 케이스 | 기대 결과 | 결과 |
|---|---|---|
| `GET /api/rooms`, `X-Member-Id: 1` | 참여 방 3개와 초대 대기 수 반환 | PASS, HTTP 200 |
| `GET /api/rooms`, header 없음 | 한국어 인증 오류 | PASS, HTTP 401 |
| `GET /api/rooms`, invalid member id | 한국어 인증 오류 | PASS, HTTP 401 |

## Browser Smoke

확인 화면: `docs/qa/issue-5/sidebar-room-list-smoke.png`

| 항목 | 결과 |
|---|---|
| 홈 진입 시 최상단 방 자동 선택 | PASS |
| 선택 방 하위 메뉴 노출 | PASS |
| 방 리스트 페이지 이동 | PASS |
| 방 생성/초대 받은 방/참여 방/방 관리 골격 노출 | PASS |
| 다른 방 선택 후 채팅 placeholder에서 선택 방 컨텍스트 유지 | PASS |
| 설정 메뉴 진입 및 기존 설정 화면 유지 | PASS |

## Public Repository Check

| 항목 | 결과 |
|---|---|
| 공개 저장소 금지 식별 문자열 | PASS, 발견 없음 |
| 실제 secret/API key/webhook URL 노출 | PASS, 발견 없음 |

## Notes

- 방 생성, 초대 수락/거절, 방 관리 저장은 이번 이슈 범위가 아니므로 골격 버튼과 안내로 제한했다.
- 채팅/추억/미션/편지 상세 기능은 각각 후속 이슈에서 구현한다.
- unread chat count와 pending mission count는 상세 기능 구현 전까지 seed 기반 표시값으로 둔다.
