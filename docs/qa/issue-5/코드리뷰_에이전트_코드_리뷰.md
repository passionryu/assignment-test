# Code Review Report

- 검토 일시: 2026-07-31 17:19 KST
- 대상 Commit: `5e9ca3c`
- 대상 Issue: #5
- 재검토 배경: Human QA FAIL 이후 선택 방 하위 메뉴 위치 유지 및 백엔드 구조/JPA 전환 반영
- 검토 범위: 코드/구조/요구사항 리뷰만 수행
- 전체 결과: PASS

## Findings

| ID | Severity | Area | Status | Description |
| --- | --- | --- | --- | --- |
| CR-005-001 | MINOR | Room badge seed value | ACCEPTED | `unreadChatCount`, `pendingMissionCount`는 상세 기능 구현 전까지 seed 기반 표시값으로 계산된다. 이번 이슈의 사이드바 골격과 뱃지 표시 요구에는 부합하며, 실제 채팅/미션 이슈에서 도메인 데이터 기반으로 교체한다. |
| CR-005-002 | MINOR | Placeholder actions | ACCEPTED | 방 생성, 초대 받은 방, 방 관리 버튼은 실제 저장/수락/수정 API 없이 골격만 제공한다. Issue #5 범위는 방 리스트 및 사이드바 구현이므로 수용한다. |
| CR-005-003 | MINOR | Notification default upsert | ACCEPTED | `getNotificationSettings`는 알림 설정 row가 없으면 기본값을 생성하는 기존 동작을 유지한다. 현재 seed가 기본 row를 준비하고 smoke API도 통과했다. 후속 리팩토링 시 read path와 write path를 분리하면 더 명확하다. |

BLOCKER 또는 CRITICAL 코드/구조 이슈는 없다.

## Backend Structure Review

| Requirement | Status | Evidence |
| --- | --- | --- |
| `member` 하위 구조 | PASS | `controller`, `model`, `repository`, `service` 하위 패키지로 정리됨 |
| `room` 하위 구조 | PASS | `controller`, `model`, `repository`, `service` 하위 패키지로 정리됨 |
| `health` 하위 구조 | PASS | `health/controller/HealthController.kt`로 이동됨 |
| import/package 경로 일관성 | PASS | Controller/Service/Repository import가 새 패키지 기준으로 컴파일됨 |
| 불필요한 flat 파일 제거 | PASS | 기존 `member/MemberRepository.kt`, `room/RoomRepository.kt` 등 flat 파일은 tracked file 기준 남아 있지 않음 |

## JPA + QueryDSL Review

| Requirement | Status | Evidence |
| --- | --- | --- |
| Gradle 설정 build 가능 | PASS | `kotlin("kapt")`, `plugin.jpa`, `spring-boot-starter-data-jpa`, QueryDSL jakarta 설정으로 `clean bootJar` 통과 |
| QueryDSL Q class 생성 | PASS | `kaptKotlin` 실행 및 `QMemberEntity`, `QRoomEntity`, `QRoomMemberEntity`, `QRoomInvitationEntity`, `QNotificationSettingsEntity` 생성 확인 |
| Flyway schema와 entity mapping | PASS | `members`, `rooms`, `room_members`, `room_invitations`, `notification_settings` 컬럼 매핑이 현재 schema와 맞음 |
| 기존 API 응답 shape 유지 | PASS | DTO `MemberModels`, `RoomModels`가 유지되고 smoke API shape 검증 통과 |
| 과도한 우회/임시 코드 여부 | PASS | 조회는 QueryDSL 조인/집계, 수정은 Spring Data JPA save로 단순하게 구성됨 |

## Regression Review

| API / Feature | Status | Evidence |
| --- | --- | --- |
| `GET /api/health` | PASS | HTTP 200, `status/database` shape 유지 |
| `GET /api/rooms` | PASS | HTTP 200, 참여 방 목록과 `pendingInvitationCount` shape 유지 |
| `GET /api/members/me` | PASS | HTTP 200, 프로필 shape 유지 |
| `PATCH /api/members/me/profile` | PASS | HTTP 200, 이름/프로필 이미지 수정 경로 유지 |
| `POST /api/members/me/password` | PASS | HTTP 200, 기존 MVP 비밀번호 변경 경로 유지 |
| `GET /api/members/me/notification-settings` | PASS | HTTP 200, 알림 설정 shape 유지 |
| `PUT /api/members/me/notification-settings` | PASS | HTTP 200, 알림 설정 저장 경로 유지 |

## Frontend Sidebar Review

| Requirement | Status | Evidence |
| --- | --- | --- |
| 선택한 방이 최상단으로 이동하지 않음 | PASS | `rooms.map` 순서 그대로 렌더링하고 선택 방만 `room-selected`로 표시 |
| 선택한 방 기존 위치 바로 아래 하위 메뉴 노출 | PASS | 각 `room-entry` 내부에서 선택된 방일 때만 `room-submenu`를 렌더링 |
| 최초 진입 시 최상단 방 기본 선택 유지 | PASS | `loadInitialData`에서 기존 선택이 없으면 `roomsResponse.rooms[0]?.id`를 선택 |
| 방 내부 placeholder 유지 | PASS | 채팅/추억/미션/편지는 `RoomFeatureView` placeholder로 제한 |
| 설정 진입 유지 | PASS | 사이드바 하단 설정 버튼과 `SettingsView` 유지 |

## Document Review

| Document | Status | Evidence |
| --- | --- | --- |
| 백엔드 도메인 패키지 구조 기준 | PASS | `docs/conventions/coding-convention.md`, `docs/dev-spec/DEV-001-tech-architecture.md`가 구조 기준을 반영 |
| JPA + QueryDSL 기준 | PASS | 기술 아키텍처 문서에 JPA + QueryDSL 기준이 반영됨 |
| Tech Lead 1차 검증 최신화 | PASS | `docs/qa/issue-5/테크리드_에이전트_1차검증.md`에 Human QA 수정 후 재검증 섹션 존재 |

## Verification Commands

```bash
./gradlew clean bootJar --no-daemon

npm run build

git diff --check
```

추가 smoke 검증:

```text
GET /api/health
GET /api/rooms
GET /api/members/me
PATCH /api/members/me/profile
POST /api/members/me/password
GET /api/members/me/notification-settings
PUT /api/members/me/notification-settings
```

## Verification Result

- `./gradlew clean bootJar --no-daemon`: PASS
- QueryDSL Q class 생성: PASS
- `npm run build`: PASS
- `git diff --check`: PASS
- 핵심 API smoke: PASS
- DB 변경 원복: PASS, 비밀번호 smoke 후 기존 seed hash 복구
- 화면 증적: `docs/qa/issue-5/sidebar-room-inline-expand-fix.png`

## Review Decision

Issue #5 Human QA 수정 커밋은 코드/구조 Review 기준을 통과했다.

AI QA 단계로 이동 가능하다.
