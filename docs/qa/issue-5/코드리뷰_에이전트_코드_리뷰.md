# Code Review Report

- 검토 일시: 2026-07-31 16:28 KST
- 대상 Commit: `dfbf855`
- 대상 Issue: #5
- 검토 범위: 코드/요구사항 리뷰만 수행
- 전체 결과: PASS

## Findings

| ID | Severity | Area | Status | Description |
| --- | --- | --- | --- | --- |
| CR-005-001 | MINOR | Room badge seed value | ACCEPTED | `unreadChatCount`, `pendingMissionCount`는 상세 기능 구현 전까지 seed 기반 표시값으로 계산된다. 이번 이슈의 사이드바 골격과 뱃지 표시 요구에는 부합하며, 실제 채팅/미션 이슈에서 도메인 데이터 기반으로 교체한다. |
| CR-005-002 | MINOR | Placeholder actions | ACCEPTED | 방 생성, 초대 받은 방, 방 관리 버튼은 실제 저장/수락/수정 API 없이 골격만 제공한다. Issue #5 범위는 방 리스트 및 사이드바 구현이므로 수용한다. |

BLOCKER 또는 CRITICAL 코드/요구사항 이슈는 없다.

## Requirement Review

| Requirement | Status | Evidence |
| --- | --- | --- |
| `GET /api/rooms` 최소 API | PASS | 현재 회원이 참여 중인 방 목록과 대기 초대 수만 반환한다. 생성/수정/삭제 API는 추가하지 않았다. |
| 사이드바가 방 목록 기반으로 동작 | PASS | 앱 초기 로딩 시 `/rooms`를 호출하고 `rooms` 상태를 `Sidebar`에 전달한다. |
| 최상단 방 기본 선택 | PASS | `loadInitialData`에서 선택 방이 없으면 `roomsResponse.rooms[0]?.id`를 기본 선택한다. |
| 선택 방 하위 메뉴 노출 | PASS | 선택 방 아래에 채팅, 추억 게시판, 미션 인증, 편지 메뉴를 노출한다. |
| 방 리스트 페이지 골격 | PASS | 방 생성, 초대 받은 방, 참여 방, 방 관리 진입 골격과 참여 방 카드 목록을 제공한다. |
| 방 내부 기능 placeholder 범위 | PASS | 채팅/추억/미션/편지는 `RoomFeatureView` placeholder로 제한했고 상세 CRUD는 추가하지 않았다. |
| 설정 진입 유지 | PASS | 사이드바 하단 설정 버튼과 `SettingsView`가 유지된다. |
| 선택 방 컨텍스트 유지 | PASS | 다른 방 선택 시 `selectedRoomId`를 변경하고 현재 뷰가 방 내부 기능이면 해당 뷰를 유지한다. |
| 기존 #4 설정 화면 명백한 회귀 없음 | PASS | 프로필 수정, 비밀번호 변경, 알림 토글, 로그아웃 모달 핸들러와 API 호출 경로가 유지된다. |

## Changed File Review

| File | Review |
| --- | --- |
| `service/server/src/main/kotlin/com/recordroom/room/RoomController.kt` | `GET /api/rooms` 단일 조회 endpoint만 추가했다. MVP 인증 헤더는 기존 `CurrentMemberResolver`를 재사용한다. |
| `service/server/src/main/kotlin/com/recordroom/room/RoomService.kt` | 회원 존재 확인 후 참여 방과 초대 대기 수를 조회하는 단순 orchestration이다. |
| `service/server/src/main/kotlin/com/recordroom/room/RoomRepository.kt` | 참여 중인 방만 조회하도록 `room_members.left_at is null`, `rooms.archived_at is null` 조건을 사용한다. |
| `service/server/src/main/kotlin/com/recordroom/room/RoomModels.kt` | 사이드바와 방 리스트에 필요한 최소 응답 모델이다. |
| `service/client/src/main.tsx` | 홈/방 리스트/방 내부 placeholder/설정을 view 단위로 분리하고 선택 방 상태를 유지한다. |
| `service/client/src/styles.css` | 사이드바, 방 리스트, placeholder, 설정 화면 레이아웃 스타일을 추가했다. |

## Verification Commands

```bash
npm run build

./gradlew test bootJar --no-daemon

git diff --check
```

## Verification Result

- `npm run build`: PASS
- `./gradlew test bootJar --no-daemon`: PASS
- `git diff --check`: PASS
- 화면 증적: `docs/qa/issue-5/sidebar-room-list-smoke.png`

## Review Decision

Issue #5는 코드/요구사항 Review 기준을 통과했다.

AI QA 단계로 이동 가능하다.
